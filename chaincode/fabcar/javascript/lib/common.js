const RequestStatus = {
  sent: "sent",
  rejected: "rejected",
  rejection_sent: "rejection_sent",
  dispatched: "dispatched",
  received: "received",
  preparing: "preparing",
}

module.exports = {
  RequestStatus
}
